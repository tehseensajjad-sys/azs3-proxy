
# file: rabata-get-1MiB.yml
warp:
  api: v1
  benchmark: get

  quiet: false
  no-color: false
  json: false

  region: "{{.Region}}"
  access-key: "{{.AccessKey}}"
  secret-key: "{{.SecretKey}}"
  host: "{{.Host}}"
  tls: {{.TLS}}

  bucket: "{{.Bucket}}"
  bench-data: "proxy-get-1MiB.csv.zst"

  params:
    duration: 300s
    concurrent: 8

    obj:
      size: 1MiB
      rand-size: false
