
# file: small-128KiB.yml
warp:
  api: v1
  benchmark: put

  quiet: false
  no-color: false
  json: false

  region: "{{.Region}}"
  access-key: "{{.AccessKey}}"
  secret-key: "{{.SecretKey}}"
  host: "{{.Host}}"
  tls: {{.TLS}}

  bucket: "{{.Bucket}}"
  bench-data: "{{.BenchData}}"

  params:
    duration: 180s
    concurrent: {{.Concurrent}}
    post: false

    obj:
      size: 128KiB
      rand-size: false
